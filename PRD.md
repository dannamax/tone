# SeekerHub 产品需求文档（PRD）

| | |
|---|---|
| 产品名称 | SeekerHub |
| 域名 | gotseeker.com |
| 版本 | V1.0（海外首发版） |
| 平台 | iOS（App Store 全球发行，不含中国大陆/香港/台湾） |
| 文档状态 | 随代码演进，最新实现以仓库为准 |
| 最后更新 | 2026-08-25 |

---

## 1. 产品概述

### 1.1 一句话定位

**SeekerHub 是一个基于地理位置的悬赏任务市场：任何人在身边发布任务、设置赏金，附近的猎人接单完成、赚取报酬。**

> "Post a task near you. A hunter nearby gets it done."

### 1.2 产品愿景

把"找人帮忙"变成像打车一样简单：任务发布者（Seeker 的需求方）用手机描述需求并付出赏金，方圆数公里内的接单人（Hunter）实时看到、就近领取、现场完成、拍照取证、即时结算。让本地微小服务（跑腿、代排队、拍照取证、简单劳务）拥有一个无需认识、无需谈判的交易场所。

### 1.3 目标市场

- **首发市场**：全球英语市场（美国、欧洲、东南亚、日韩、澳新等），App 为英文单语版本。
- **明确排除**：中国大陆、香港、台湾（首发阶段不开放下载区域）。
- **基础设施**：服务部署于香港（免备案、覆盖全球延迟均衡），支付与合规按海外标准设计。

### 1.4 与传统众包/跑腿平台的差异

| 维度 | 传统平台（TaskRabbit、跑腿 App） | SeekerHub |
|---|---|---|
| 供给方 | 平台认证的骑手/服务商 | 任何用户（双边角色可互换） |
| 定价 | 平台估价/议价 | 发布者自主定价（$1–$200） |
| 门槛 | 注册审核、押金、培训 | 一个邮箱即可，发布消耗额度 |
| 取证 | 系统黑箱 | 照片 + GPS 水印，双方可见 |
| 结算 | 平台代发薪 | V1 额度制 / V2 持牌支付机构分账 |

---

## 2. 用户与场景

### 2.1 双边用户模型

同一用户可随时在两个角色间切换，账号互通：

**Publisher（任务发布者 / Seeker）**
- 有本地需求、愿意付赏金的人
- 付费购买发布额度，是平台收入来源

**Hunter（猎人 / 任务领取者）**
- 有空闲时间、位于任务半径内的人
- 完成任务赚取赏金/报酬，是供给与留存来源

### 2.2 典型用户画像

| 画像 | 描述 | 核心诉求 |
|---|---|---|
| 都市上班族 Lily，28 | 加班到 21 点，想让附近的人帮忙排队买一杯奶茶 | 快、便宜、可信 |
| 自由职业者 Max，24 | 时间碎片化，想顺手接几个 5 分钟小任务赚零花 | 就近、简单、确认有钱拿 |
| 小微店主 Anna，35 | 想确认隔壁街竞争店铺的陈列情况 | 匿名、有照片证据、有定位 |

### 2.3 核心使用场景（User Journey）

**发布者旅程：**
```
注册（邮箱验证码）→ 获赠 3 张发布额度 → 填写任务
（标题/描述/地图选点/半径/时限/赏金）→ 发布（扣 1 张额度）
→ 任务进入附近猎人的广场 → 实时聊天/通知 → 收到完成证据
→ 确认验收 → 赏金划转 → 评价结束
```

**猎人旅程：**
```
注册 → 打开广场（按距离排序）→ 领取任务（并发 ≤3）
→ 前往地点 → 拍照 + 定位提交证据（需在半径内）
→ 等待发布者确认 → 收到赏金/通知 → 继续接单
```

**异常分支：**
- 发布者撤回（未被领取）：任务下架，额度自动回收
- 猎人放弃（已领取未提交）：任务回到已发布状态，可被他人领取
- 提交后争议：任务进入 Disputed，平台仲裁 → 确认放款 或 退款给发布者
- 任务超时（发布后 2 小时无人领取）：过期下架

---

## 3. 功能需求

### 3.1 功能架构总览

```
SeekerHub App
├── 认证模块      邮箱验证码登录/注册、JWT、设备绑定
├── 任务广场      LBS 发现（附近/全部）、距离排序
├── 任务发布      表单 + 地图选点 + 额度消耗
├── 任务履约      领取 → 拍照取证（GPS 水印）→ 提交
├── 审核结算      确认/异议、赏金划转、争议仲裁
├── 任务聊天      WebSocket 实时消息 + 图片（仅平台存储）
├── 钱包          余额/冻结/流水/提现
├── 发布额度      IAP 购买、消耗、回收
├── 通知中心      站内通知、实时推送（WS）
└── 设置与隐私    语言、账号注销（GDPR）
```

### 3.2 账号与认证

| 需求 | 规则 |
|---|---|
| 注册/登录 | 邮箱 + 6 位验证码，验证码 5 分钟有效，Resend 邮件通道 |
| 防滥用 | 验证码速率限制、尝试次数上限、设备 ID 绑定 |
| 会话 | JWT Bearer Token |
| 新用户权益 | 赠送 3 张发布额度（FreeQuota=3），默认昵称 Hunter+序号 |

### 3.3 任务广场（发现页）

| 需求 | 规则 |
|---|---|
| 列表范围 | 全部已发布任务，或按距离过滤（1/3/5/10 km / 全部） |
| 排序 | 按距用户当前位置的球面距离升序（Haversine）；未定位时按最新 |
| 卡片信息 | 标题、赏金金额+货币、状态、距离、时限、地点 |
| 刷新 | 下拉刷新 + 发布/领取后全局触发刷新 |

### 3.4 任务发布

| 字段 | 规则（后端强校验） |
|---|---|
| 标题 | 必填，1–50 字符 |
| 描述 | 可选，≤200 字符，禁止包含链接 |
| 目标位置 | 地图选点（可拖拽）或当前位置，反解地址；必须有坐标 |
| 可见半径 | 1 / 3 / 5 / 10 km 四档 |
| 完成时限 | 5 / 15 / 30 / 60 / 120 分钟五档 |
| 赏金 | 1–200（CNY 或 USD） |
| 发布确认 | 位置若为"自动填充的当前位置"，弹窗让发布者确认 |
| 成本 | 消耗 1 张发布额度；额度不足返回 402 引导购买 |

### 3.5 任务领取与完成（履约）

| 需求 | 规则 |
|---|---|
| 领取条件 | 任务状态为 published；不能领取自己发布的任务；同时进行的任务 ≤3 个 |
| 领取 | 原子操作，并发安全（SQL 条件更新防双领） |
| 证据提交 | 最多 3 张照片（JPEG/PNG/GIF/WebP，单张 ≤5MB，魔数校验）+ 文字备注（禁链接）+ 提交时定位 |
| 位置校验 | 提交坐标必须在任务半径内（服务端校验），照片带 GPS 水印 |
| 过期 | 发布后 2 小时无人领取自动过期；领取后超时未提交可被发布者处理 |

### 3.6 审核与结算

| 需求 | 规则 |
|---|---|
| 发布者确认 | 证据页查看 → 确认 → 赏金划转给猎人（写流水） |
| 提出异议 | 进入争议流程，附理由 |
| 自动确认 | 提交后 24 小时未处理自动确认（保护猎人） |
| 撤回 | 仅未被领取时可撤回，额度回收 |
| 放弃 | 猎人主动放弃，任务回到已发布状态 |

### 3.7 争议处理

| 需求 | 规则 |
|---|---|
| 发起 | 发布者对提交证据不满意时提交 Dispute（理由必填 ≤500 字） |
| 处理 | 平台仲裁 → 放款（确认完成）或 退款（赏金退回发布者） |
| 状态机 | published → claimed → submitted → completed / disputed → refunded |

### 3.8 任务内沟通（Chat）

| 需求 | 规则 |
|---|---|
| 消息类型 | 文字 + 图片，WebSocket 实时 |
| 图片安全 | 仅允许平台对象存储（COS）URL，白名单校验 |
| 反诈钓 | 文字内容禁止包含任何 URL（服务端校验拒绝） |
| 会话归属 | 仅任务双方可见 |

### 3.9 钱包与发布额度

**钱包（V1 以展示+提现为主）：**

| 需求 | 规则 |
|---|---|
| 资产展示 | 可用余额、冻结金额、累计获得/支出 |
| 流水 | 全类型交易记录（赏金、退款、额度购买/消耗） |
| 提现 | ≥10 起，微信/支付宝渠道，T+1 到账（人工审核期） |

**发布额度（V1 核心变现）：** 见第 4 章。

### 3.10 通知中心

| 需求 | 规则 |
|---|---|
| 站内通知 | 任务被领取/证据已提交/赏金到账/任务争议/退款等 |
| 实时性 | WebSocket 在线实时推送，离线进通知列表 |
| 已读 | 单条/全部标记已读 |

### 3.11 设置与隐私

| 需求 | 规则 |
|---|---|
| 语言 | English（全球版单语） |
| 权限说明 | 相机（完成任务取证）、定位（任务匹配+水印） |
| 账号注销 | GDPR 流程，注销后 30 天内删除/匿名化个人数据 |
| 合规页 | 隐私政策/服务条款（gotseeker.com/privacy、/terms） |
| 加密出口 | ITSAppUsesNonExemptEncryption=false（HTTPS 豁免） |

---

## 4. 商业模式（V1：IAP 额度制）

### 4.1 经营逻辑

**把"任务上架权"做成消耗型数字商品，经 Apple IAP 销售。**

```
用户 ──$0.99──→ Apple（抽成 30%）──结算──→ 开发者账户
                     │
                     ▼
            发布额度 ×2（consumable IAP）
                     │
        发布任务 ──消耗 1──→ 撤回 ──回收 1──→ 可复用
```

平台**不触碰任何用户间资金**：赏金（Bounty）在 V1 中是履约凭证记账，真钱支付能力规划在 V2（Stripe Connect 托管分账）。

### 4.2 定价体系（App Store Connect 已配置 SKU）

| 套餐 | 价格 | 额度 | 折合单价 | SKU（消耗型 IAP） |
|---|---|---|---|---|
| Starter | $0.99 | 2 张 | $0.50/张 | pkg_usd_2 |
| Pro | $4.99 | 10 张 | $0.50/张 | pkg_usd_10 |
| 新人礼 | 免费 | 3 张 | — | 注册赠送 |

> 定价杠杆预留：大包折扣（提客单价）、限时促销、区域定价。

### 4.3 为什么是额度制（合规架构）

| 设计目标 | 解法 |
|---|---|
| 个人开发者无支付牌照 | 平台零资金沉淀，仅经 Apple IAP 销售数字商品 |
| 反垃圾任务 | 发布花真钱（买额度），成本前置抑制滥用 |
| iOS 合规 | 消耗型 IAP（额度会被消耗），符合 App Store 3.1.1 |
| 变现闭环 | Apple 作为记录商户（MoR）处理全部支付、税务、退款 |

### 4.4 V2 规划（已完成的合规调研）

注册香港主体（个体户 BR 或有限公司）+ Stripe HK + **Stripe Connect** 担保交易：

```
发布者卡 ──预授权冻结(赏金+费)──→ Stripe 托管
确认完成 ──分账──→ 猎人 Connect 账户（自动 payout 到卡）
              └──→ 平台账户（仅收手续费，零资金池）
```

约束（来自 Stripe 官方受限业务清单）：marketplace 须用 Connect 并如实申报；P2P 自由转账禁止；刷量/互动类任务禁止。

---

## 5. 竞品分析

| 竞品 | 模式 | SeekerHub 差异点 |
|---|---|---|
| TaskRabbit（美） | 认证自由职业者，平台抽成 15%+，App 内支付 | 无认证门槛，人人可接单；轻量 5 分钟级小任务 |
| Fiverr / Upwork | 全球远程服务市场，按项目 | 本地即时（LBS 半径内），非远程 |
| 闲鱼跑腿/滴滴跑腿 | 专职骑手配送 | 双边可互换，任务类型开放 |
| Field Agent / Mobee | 众包数据采集（拍照任务） | 取证机制相似，但任务类型开放、定价自由 |

**核心壁垒假设**：双边密度（同半径内任务足够多、猎人足够近）+ 取证信任链（照片+GPS+时间戳）。

---

## 6. 非功能需求

### 6.1 性能与可用性

| 指标 | 目标 |
|---|---|
| API P95 延迟 | < 300ms（HK 节点覆盖全球） |
| 可用性 | 单节点 99.5%（V1），故障自动回滚镜像 |
| 部署 | CI→CD 全自动（GitHub Actions），发布即版本核对（healthz version=commit SHA） |
| 图片加载 | 对象存储直链 + CDN（腾讯云 COS） |

### 6.2 安全

| 威胁 | 防护（已实现） |
|---|---|
| 恶意上传 | MIME 白名单 + 魔数嗅探 + 5MB 限制 + 强制 image/* Content-Type |
| 存储型 XSS | 上传对象强制 Content-Disposition: inline + 禁路径穿越 |
| 钓鱼/外链滥用 | 聊天与备注禁 URL；图片仅限平台存储域名白名单 |
| 双领竞态 | SQL 条件更新（status=published）原子领取 |
| 重复发放/支付 | IAP 以 transaction_id 幂等；JWS 本地验签（Apple 根证书链） |
| 接口滥用 | JWT 鉴权 + 速率限制 + 设备绑定 |

### 6.3 合规与隐私

| 项 | 状态 |
|---|---|
| 隐私政策/条款 | 英文版公网可访问（gotseeker.com） |
| GDPR | 首启同意流（不可绕过）+ 账号注销 30 天删除 |
| 隐私清单 | PrivacyInfo.xcprivacy（位置/相机/UserDefaults 等申报，不追踪） |
| 数据存储 | 香港（HK）云服务器，TLS 传输加密 |
| 支付 | V1 全部经 Apple IAP，平台零用户资金 |
| 出口加密 | 标准豁免申报 |

---

## 7. 技术架构概要

```
iOS App（SwiftUI + StoreKit 2）
        │ HTTPS / WSS
        ▼
Go 后端（Gin）─ 腾讯云 HK CVM（Docker Compose）
  ├─ SQLite（用户/任务/流水）+ Redis（验证码/会话）
  ├─ 对象存储：腾讯云 COS（图片，桶私有+对象公有读）
  ├─ 邮件：Resend API（验证码）
  └─ WebSocket Hub（实时通知/聊天）
        ▲
GitHub main ── CI（vet/build/test）── CD（SSH 部署 + 版本核对 + 失败回滚）
```

关键工程决策：发布版本可核对（GIT_SHA 编译进二进制）、机器可读错误码（i18n 英文）、e2e 冒烟脚本（29 项全链路）、CD-only 发布纪律（禁止手动上生产）。

---

## 8. 产品路线图

| 阶段 | 主题 | 内容 |
|---|---|---|
| **V1.0（当前，待提审）** | 英文全球首发 | IAP 额度制闭环、LBS 任务全流程、取证与争议、聊天、通知、GDPR |
| V1.1 | 运营基建 | 任务管理（发布者编辑/删除任务）、内容审核（任务文本合规）、数据分析埋点 |
| V1.2 | 增长 | 评价/信用体系、猎人等级、推送唤醒（APNs）、推荐奖励额度 |
| **V2.0** | 真金闭环 | 香港/美国主体注册 + Stripe Connect：赏金预授权冻结→确认分账→猎人自动 payout；额度降级为发布门槛 |
| V2.x | 规模化 | Postgres 迁移、多区域部署、反欺诈风控、Web 端 |

---

## 9. 成功指标（KPI）

| 分类 | 指标 | V1 目标（上线 3 个月） |
|---|---|---|
| 活跃 | WAU / 发布任务数 / 完成任务数 | 验证双边密度模型 |
| 转化 | 注册→首发布 / 额度用尽→付费购买 | ≥8% 付费转化 |
| 供给健康 | 任务领取中位时长 / 完成/发布比 | <30min / >60% |
| 信任 | 争议率 / 自动确认占比 | <5% / 监控 |
| 变现 | IAP 收入 / ARPPU | 覆盖基础设施成本 |

---

## 10. 风险与应对

| 风险 | 等级 | 应对 |
|---|---|---|
| 双边冷启动（密度不足） | 高 | 新人赠额度、聚焦单一城市/校园启动、5 分钟级轻任务切入 |
| 任务内容违规（涉违法/刷量） | 高 | 发布需额度（成本前置）+ 文本审核接入（V1.1 腾讯 CI）+ 举报通道 |
| IAP 审核被拒（额度型商品） | 中 | 确认 Connect 后台为消耗型；用途说明清晰（发布权限而非货币） |
| 真钱模式合规（V2） | 中 | 仅 Stripe Connect 官方资金流；额度永不可提现；市场模式如实申报 |
| 单点故障 | 低 | CD 自动回滚 + 备份镜像；DB 迁移 Postgres 在路线图 |

---

*本文档与代码仓库同步维护：`README.md`（快速开始）、`ARCHITECTURE.md`（技术细节）、`TODO.md`（执行清单）、`RELEASE.md`（发布规范）。*
