# SeekerHub 产品需求文档（PRD）

| | |
|---|---|
| 产品名称 | SeekerHub |
| 域名 | gotseeker.com |
| 版本 | V1.0（海外首发版） |
| 平台 | iOS（App Store 全球发行，不含中国大陆/香港/台湾） |
| 文档状态 | 随代码演进，最新实现以仓库为准 |
| 最后更新 | 2026-08-26（金豆经济版） |

---

## 1. 产品概述

### 1.1 一句话定位

**SeekerHub 是一个基于地理位置的悬赏任务市场：任何人在身边发布任务、押上赏金，附近的猎人接单完成、赚取金豆。**

> "Post a task near you. A hunter nearby gets it done."

### 1.2 产品愿景

把"找人帮忙"变成像打车一样简单：任务发布者（Seeker 的需求方）用手机描述需求并押上赏金，方圆数公里内的接单人（Hunter）实时看到、就近领取、现场完成、拍照取证、即时结算。让本地微小服务（跑腿、代排队、拍照取证、简单劳务）拥有一个无需认识、无需谈判的交易场所。

### 1.3 目标市场

- **首发市场**：全球英语市场（美国、欧洲、东南亚、日韩、澳新等），App 为英文单语版本。
- **明确排除**：中国大陆、香港、台湾（首发阶段不开放下载区域）。
- **基础设施**：服务部署于香港（免备案、覆盖全球延迟均衡），支付与合规按海外标准设计。

### 1.4 与传统众包/跑腿平台的差异

| 维度 | 传统平台（TaskRabbit、跑腿 App） | SeekerHub |
|---|---|---|
| 供给方 | 平台认证的骑手/服务商 | 任何用户（双边角色可互换） |
| 定价 | 平台估价/议价 | 发布者自主定价（5–50 Beans） |
| 门槛 | 注册审核、押金、培训 | 一个邮箱即可，发布消耗金豆 |
| 取证 | 系统黑箱 | 照片 + GPS 水印，双方可见 |
| 结算 | 平台代发薪 | V1 金豆闭环 / V2 持牌支付机构分账 |

---

## 2. 用户与场景

### 2.1 双边用户模型

同一用户可随时在两个角色间切换，账号互通：

**Publisher（任务发布者 / Seeker）**
- 有本地需求、愿意押赏金的人
- 充值购买金豆发布任务，是平台收入来源

**Hunter（猎人 / 任务领取者）**
- 有空闲时间、位于任务半径内的人
- 完成任务赚取金豆（赚到豆可再用于发布任务），是供给与留存来源

### 2.2 典型用户画像

| 画像 | 描述 | 核心诉求 |
|---|---|---|
| 都市上班族 Lily，28 | 加班到 21 点，想让附近的人帮忙排队买一杯奶茶 | 快、便宜、可信 |
| 自由职业者 Max，24 | 时间碎片化，想顺手接几个 5 分钟小任务，赚豆后发布自己的小需求 | 就近、简单、确认有豆拿 |
| 小微店主 Anna，35 | 想确认隔壁街竞争店铺的陈列情况 | 匿名、有照片证据、有定位 |

### 2.3 核心用户旅程（金豆经济版）

**发布者旅程：**
```
注册（邮箱验证码）→ 获赠 5 金豆注册礼 → 填写任务
（标题/描述/地图选点/半径/时限/赏金档位）→ 发布（预扣金豆）
→ 任务进入附近猎人的广场 → 实时聊天/通知 → 收到完成证据
→ 确认验收 → 赏金金豆划转给猎人 → （豆尽 → 充值 → 循环）
```

**猎人旅程：**
```
注册 → 打开广场（距离/金豆/最新三种排序）→ 领取任务（并发 ≤3）
→ 前往地点 → 拍照 + 定位提交证据（需在半径内）
→ 等待发布者确认 → 金豆到账（beans_earned）→ 可继续接单或转发布
```

**异常分支：**
- 发布者取消（未被领取）：任务下架，金豆自动退回
- 猎人放弃（已领取未提交）：任务回到已发布状态，可被他人领取
- 提交后争议：任务进入 Disputed，仲裁 → 确认放款 或 退款给发布者

---

## 3. 完整客户使用流程说明（Product Walkthrough）

> 本章面向客户演示/介绍：从一个新用户下载 App 开始，到完成"发布—接单—交付—结算"完整闭环的端到端体验。每一步均已在生产环境验证通过。

### 3.1 首次启动与账号准备（约 1 分钟）

1. **下载安装**：App Store 搜索 SeekerHub 安装（首发全球英语区）。
2. **隐私同意（GDPR）**：首次打开弹出隐私同意页，展示《隐私政策》与《服务条款》全文链接（应用内可直接阅读），点击 **Agree** 继续——不同意则无法使用（合规要求，不可绕过）。
3. **语言选择**：Onboarding 页选择语言（V1 为 English），点击 **Get Started**。
4. **注册/登录**：输入邮箱 → 点击 **Get Code** 收到 6 位验证码邮件（Resend 通道，60 秒内不可重发）→ 输入验证码 → 登录成功。
   - 新用户自动注册并**获赠 5 金豆注册礼**（够发布一个最低赏金任务，"先尝后买"）。
   - 无需密码、无需绑定手机，一个邮箱即完成。

### 3.2 逛任务广场（发现任务）

登录后进入主界面，底部四个 Tab：**Square（广场）/ My Tasks（我的任务）/ Messages（消息）/ Profile（我的）**，中央悬浮 **+** 按钮随时发布任务。

广场列表卡片包含：**标题、赏金金豆数、距离、完成时限、地点**。三种排序方式一键切换：

| 排序 | 用途 |
|---|---|
| **Distance（距离）** | 默认，找身边最近的任务（Haversine 球面距离） |
| **Beans（金豆）** | 按赏金从高到低，高价值任务优先 |
| **Newest（最新）** | 刚发布的任务优先 |

距离过滤支持 1 / 3 / 5 / 10 km 或全部。

### 3.3 发布一个任务（需求方视角）

点击中央 **+** 按钮打开发布页：

1. **标题**（必填，1–50 字符）与**描述**（可选，≤200 字符，禁止链接防诈钓）。
2. **设定赏金（Bounty Beans）**：金豆档位快捷选择 **5 / 10 / 20 / 30 / 50**，或用 +/- 步进器微调（区间 5–50 豆）；界面实时显示消耗 "Cost: N beans"。
3. **选择位置**：拖拽地图选点或自动采用当前位置（自动填充会弹窗请发布者确认），反解析为街道地址。
4. **设置可见半径**（1/3/5/10 km）与**完成时限**（5/15/30/60/120 分钟）。
5. 点击 **Publish Task** → 系统即时**原子预扣金豆**（余额不足则返回引导充值）→ 任务上架，进入半径内所有猎人的广场。

> 充值引导：金豆不足时（402 Not enough beans），一键跳转钱包购买（见 3.6）。

### 3.4 接一个任务（供给方视角）

1. 在广场浏览（建议按 Distance 排序找最近的）。
2. 点击任务卡片查看**详情**：描述、地点地图、赏金、时限、发布者。
3. 点击 **Claim 领取**：
   - 不能领取自己发布的任务；
   - 同时进行的任务 ≤3 个；
   - 原子防双领（两人同时抢，只有一人成功）。
4. 领取成功后可通过**任务内聊天**与发布者实时沟通（文字/图片，WebSocket）。

### 3.5 交付与验收（信任链核心）

**猎人交付：**
1. 在时限内前往任务地点。
2. 点击提交：**最多 3 张现场照片**（JPEG/PNG/GIF/WebP，单张 ≤5MB）+ 文字备注。
3. 系统自动附加**提交时 GPS 定位**，服务端校验必须在任务半径内；照片带地理水印。
4. 提交后任务进入 Submitted 状态，发布者收到实时通知。

**发布者验收：**
1. 收到"证据已提交"推送，进入审核页：查看照片大图、提交时间、提交位置。
2. 两种选择：
   - **确认 Confirm** → 赏金金豆即时划转给猎人（记入猎人的 earned 账本），交易完成；
   - **有异议 Dispute**（填写理由）→ 进入仲裁，结果为放款或退款。
3. 保护机制：提交后 24 小时未处理将自动确认，保障猎人权益。

### 3.6 钱包与金豆充值（IAP）

入口：**Profile（我的）→ My Wallet**，钱包页展示：

- **金豆总余额**（大字展示）与构成明细：`Purchased X · Earned Y`（充值豆 / 赚取豆）；
- **Get Beans** 按钮 → 内购充值页。

**充值套餐（App Store 消耗型内购，已在 App Store Connect 配置）：**

| 套餐 | 价格 | 金豆 | 折合单价 | 产品 ID |
|---|---|---|---|---|
| Starter | $0.99 | 5 Beans | $0.198/豆 | pkg_usd_2 |
| **Value** | **$1.99** | **10 Beans** | $0.199/豆 | **pkg_usd_5** |
| Pro | $4.99 | 20 Beans | $0.25/豆 | pkg_usd_10 |

> 高频小额设计：最低赏金 5 豆 = 最小充值包 $0.99 恰好发一个任务，用完再充，形成"发布→用尽→再充"的自然消费循环。

**流水记录**：所有金豆变动（购买/发布消耗/赏金获得/退款/注册礼）均可在交易记录中逐条查看，账目透明。

**奖励中心（Rewards Center）**：钱包页提供入口，标注 **Coming Soon**——展示兑换资格双门槛进度（**已确认任务 ≥ 50 个**，主门槛；**赚取金豆 ≥ 50 豆**，最低累计报酬下限）与规则预告：仅 earned 豆可兑换、purchased 豆不可兑换、金豆不可转让、比例与最终规则以上线公示为准（V1 不含任何提现承诺）。

### 3.7 消息、通知与履约沟通

- **Messages（消息 Tab）**：站内通知中心，任务被领取/证据已提交/金豆到账/争议/退款等事件实时推送（在线 WebSocket 即达，离线进列表），支持单条/全部已读。
- **任务内聊天**：仅任务双方可见的实时会话，可发文字与图片（图片仅限平台对象存储，文字禁止任何 URL——反诈钓）。

### 3.8 异常流程（客户需要知道的）

| 场景 | 操作 | 结果 |
|---|---|---|
| 发布后反悔（未被领取） | 发布者取消任务 | 任务下架，**金豆自动退回** |
| 猎人接了但去不了 | 猎人放弃（Abandon） | 任务回到广场可再被领取 |
| 对交付不满意 | 发布者发起争议（必填理由） | 仲裁后放款或退款 |
| 争议判定退款 | — | **金豆退回发布者** |
| 领取后超时未提交 | 发布者可处理 | 任务释放回广场 |

### 3.9 个人中心与设置

**Profile（我的）页**——精简为唯一职责入口：

```
我的
├─ 头像 / 昵称 / 用户 ID
├─ My Wallet（显示当前金豆余额）→ 充值 / 流水 / 奖励中心
├─ My Tasks（我发布的 / 我认领的）
├─ Settings（设置）
│   ├─ Language（语言）
│   ├─ Notifications（通知开关）
│   ├─ Privacy Policy / Terms of Service（应用内阅读，全 App 唯一入口）
│   └─ Version（版本信息）
├─ Help & Feedback（邮件反馈，自动附带版本号）
└─ Log Out（退出登录，带确认弹窗）
```

### 3.10 端到端旅程图（一图流）

```
┌─客户A（需求方）────────────────────────────────────────────┐
│ 下载 → 同意隐私 → 注册(赠5豆) → 发布任务(预扣5豆)          │
│      → 等待 → 收到证据 → 确认 → (豆尽) → $0.99充值5豆 → 再发布 │
└────────────────┬───────────────────────────────────────────┘
                 │ 任务出现在广场
┌─客户B（供给方）▼───────────────────────────────────────────┐
│ 注册 → 广场按距离浏览 → 领取 → 前往地点 → 拍照+定位提交      │
│      → 被确认 → 赚5豆 → 转为发布者发布自己的需求（经济内循环）│
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 功能需求

### 4.1 功能架构总览

```
SeekerHub App
├── 认证模块      邮箱验证码登录/注册、JWT、设备绑定
├── 任务广场      LBS 发现、距离/金豆/最新三种排序、半径过滤
├── 任务发布      表单 + 地图选点 + 金豆预扣（原子）
├── 任务履约      领取 → 拍照取证（GPS 水印）→ 提交
├── 审核结算      确认/异议、金豆划转、争议仲裁
├── 任务聊天      WebSocket 实时消息 + 图片（仅平台存储）
├── 钱包          金豆双账本（充值豆/赚取豆）、流水、奖励中心
├── 金豆充值      Apple IAP（消耗型），JWS 本地验签
├── 通知中心      站内通知、实时推送（WS）
└── 设置与隐私    语言、通知、合规页、账号注销（GDPR）
```

### 4.2 账号与认证

| 需求 | 规则 |
|---|---|
| 注册/登录 | 邮箱 + 6 位验证码，5 分钟有效，Resend 邮件通道 |
| 防滥用 | 60 秒发码冷却、错误次数上限、设备 ID 绑定 |
| 会话 | JWT Bearer Token |
| 新用户权益 | **赠送 5 金豆注册礼**（与最低赏金等值，先尝后买） |

### 4.3 任务广场（发现页）

| 需求 | 规则 |
|---|---|
| 列表范围 | 全部已发布任务，或按距离过滤（1/3/5/10 km / 全部） |
| 排序 | **distance（默认，球面距离升序）/ beans（赏金降序）/ newest（最新）** 三种，服务端白名单校验 |
| 卡片信息 | 标题、赏金金豆数、状态、距离、时限、地点 |
| 刷新 | 下拉刷新 + 发布/领取后全局触发刷新 |

### 4.4 任务发布

| 字段 | 规则（后端强校验） |
|---|---|
| 标题 | 必填，1–50 字符 |
| 描述 | 可选，≤200 字符，禁止包含链接 |
| 目标位置 | 地图选点（可拖拽）或当前位置，反解地址；必须有坐标 |
| 可见半径 | 1 / 3 / 5 / 10 km 四档 |
| 完成时限 | 5 / 15 / 30 / 60 / 120 分钟五档 |
| 赏金 | **5–50 金豆**（BountyBeans，整数） |
| 发布成本 | **原子预扣等额金豆**（先扣充值豆再扣赚取豆）；余额不足返回 402 引导充值 |
| 发布确认 | 位置若为"自动填充的当前位置"，弹窗让发布者确认 |
| 失败回滚 | 任务创建失败时金豆自动退回（强一致） |

### 4.5 任务领取与完成（履约）

| 需求 | 规则 |
|---|---|
| 领取条件 | 任务状态为 published；不能领取自己发布的任务；同时进行 ≤3 个 |
| 领取 | 原子操作，并发安全（SQL 条件更新防双领） |
| 证据提交 | 最多 3 张照片（JPEG/PNG/GIF/WebP，单张 ≤5MB，魔数校验）+ 文字备注（禁链接）+ 提交时定位 |
| 位置校验 | 提交坐标必须在任务半径内（服务端校验），照片带 GPS 水印 |

### 4.6 审核与结算（金豆账本）

| 需求 | 规则 |
|---|---|
| 发布者确认 | 证据页查看 → 确认 → **赏金金豆划转给猎人（earned 账本）**，写流水 |
| 提出异议 | 进入争议流程，附理由 |
| 自动确认 | 提交后 24 小时未处理自动确认（保护猎人） |
| 撤回 | 仅未被领取时可撤回，**金豆退回发布者** |
| 放弃 | 猎人主动放弃，任务回到已发布状态 |

### 4.7 争议处理

| 需求 | 规则 |
|---|---|
| 发起 | 发布者对提交证据不满意时提交 Dispute（理由必填 ≤500 字） |
| 处理 | 平台仲裁 → 放款（确认完成）或 退款（**金豆退回发布者**） |
| 状态机 | published → claimed → submitted → confirmed / disputed → refunded |

### 4.8 任务内沟通（Chat）

| 需求 | 规则 |
|---|---|
| 消息类型 | 文字 + 图片，WebSocket 实时 |
| 图片安全 | 仅允许平台对象存储（COS）URL，白名单校验 |
| 反诈钓 | 文字内容禁止包含任何 URL（服务端校验拒绝） |
| 会话归属 | 仅任务双方可见 |

### 4.9 钱包与金豆账本

**双账本设计（资金属性隔离）：**

| 账本 | 来源 | 用途 |
|---|---|---|
| **beans_purchased（充值豆）** | IAP 购买、退款返还、注册礼 | 消耗时优先扣除；不可提现 |
| **beans_earned（赚取豆）** | 完成任务获得的赏金 | 可再用于发布；V2 计划开放奖励兑换 |

| 需求 | 规则 |
|---|---|
| 余额展示 | 总豆数 + `Purchased X · Earned Y` 明细 |
| 流水 | 全类型交易记录（grant / bean_buy / bean_spend / bean_reward / bean_refund） |
| 消耗顺序 | 原子 SQL：先扣充值豆，不足部分扣赚取豆 |
| 奖励中心 | Coming Soon 占位 + 双门槛进度展示（50 任务 + 50 豆）；不做任何提现承诺表述，比例上线时公布 |

### 4.10 金豆充值（IAP）

| 需求 | 规则 |
|---|---|
| 商品类型 | Apple 消耗型 IAP（Consumable），符合 App Store 3.1.1 |
| 套餐 | $0.99/5豆、$1.99/10豆、$4.99/20豆（详见 3.6） |
| 验签 | StoreKit 2 + **JWS 本地验签**（Apple 根证书链，无需服务器中转） |
| 幂等 | 以 transaction_id 去重，重复确认不重复发豆 |
| 防伪造 | 非法收据/伪造 JWS 一律拒绝（400） |

### 4.11 通知中心

| 需求 | 规则 |
|---|---|
| 站内通知 | 任务被领取/证据已提交/金豆到账/任务争议/退款等 |
| 实时性 | WebSocket 在线实时推送，离线进通知列表 |
| 已读 | 单条/全部标记已读 |

### 4.12 设置与隐私

| 需求 | 规则 |
|---|---|
| 语言 | English（全球版单语） |
| 合规页 | 隐私政策/服务条款（应用内阅读，gotseeker.com 同步） |
| 账号注销 | GDPR 流程，注销后 30 天内删除/匿名化个人数据 |
| 权限说明 | 相机（完成任务取证）、定位（任务匹配+水印） |
| 加密出口 | ITSAppUsesNonExemptEncryption=false（HTTPS 豁免） |

---

## 5. 商业模式（V1：IAP 金豆制）

### 5.1 经营逻辑

**把"赏金"做成消耗型虚拟商品（金豆），经 Apple IAP 销售；发布任务先押金豆，确认后金豆归猎人——平台内形成完整的经济循环。**

```
用户 ──$0.99──→ Apple（抽成 30%）──结算──→ 开发者账户
                     │
                     ▼
              金豆 ×5（consumable IAP）
                     │
   发布任务 ──预扣赏金豆──→ 猎人完成 ──确认──→ 金豆归猎人
       ▲                                        │
       └────── 赚到的豆可再发布（内循环）←──────┘
       （豆尽 → 引导充值 → 新一轮消费）
```

平台**不触碰任何用户间资金**：金豆在 V1 中是发布/履约凭证记账，真金支付能力规划在 V2（Stripe Connect 托管分账）。**金豆不可提现、不可转让**。

### 5.2 定价体系（App Store Connect 已配置并可供审核）

| 套餐 | 价格 | 金豆 | 折合单价 | SKU（消耗型 IAP） |
|---|---|---|---|---|
| Starter | $0.99 | 5 豆 | $0.198/豆 | pkg_usd_2 |
| **Value** | **$1.99** | **10 豆** | $0.199/豆 | **pkg_usd_5** |
| Pro | $4.99 | 20 豆 | $0.25/豆 | pkg_usd_10 |
| 新人礼 | 免费 | 5 豆 | — | 注册赠送 |

> 高频小额设计：统一豆单价（无大包折扣），最低赏金 5 豆 = 最小充值包恰好发一个任务。用户每完成一次"发布→豆尽→充值"循环即产生一次 $0.99 消费，以高频复购替代大额客单。

### 5.3 为什么是金豆制（合规架构）

| 设计目标 | 解法 |
|---|---|
| 个人开发者无支付牌照 | 平台零资金沉淀，仅经 Apple IAP 销售数字商品 |
| 反垃圾任务 | 发布押真金豆，成本前置抑制滥用 |
| iOS 合规 | 消耗型 IAP（金豆会被消耗），符合 App Store 3.1.1 |
| 双边激励 | 猎人赚豆→可发布，双边在同一经济体系内循环 |
| 变现闭环 | Apple 作为记录商户（MoR）处理全部支付、税务、退款 |

### 5.4 V2 规划（已完成的合规调研）

注册香港主体（个体户 BR 或有限公司）+ Stripe HK + **Stripe Connect** 担保交易：

```
发布者卡 ──预授权冻结(赏金+费)──→ Stripe 托管
确认完成 ──分账──→ 猎人 Connect 账户（自动 payout 到卡）
              └──→ 平台账户（仅收手续费，零资金池）
```

约束（来自 Stripe 官方受限业务清单）：marketplace 须用 Connect 并如实申报；P2P 自由转账禁止；刷量/互动类任务禁止。

---

## 6. 竞品分析

| 竞品 | 模式 | SeekerHub 差异点 |
|---|---|---|
| TaskRabbit（美） | 认证自由职业者，平台抽成 15%+，App 内支付 | 无认证门槛，人人可接单；轻量 5 分钟级小任务 |
| Fiverr / Upwork | 全球远程服务市场，按项目 | 本地即时（LBS 半径内），非远程 |
| 闲鱼跑腿/滴滴跑腿 | 专职骑手配送 | 双边可互换，任务类型开放 |
| Field Agent / Mobee | 众包数据采集（拍照任务） | 取证机制相似，但任务类型开放、定价自由 |

**核心壁垒假设**：双边密度（同半径内任务足够多、猎人足够近）+ 取证信任链（照片+GPS+时间戳）。

---

## 7. 非功能需求

### 7.1 性能与可用性

| 指标 | 目标 |
|---|---|
| API P95 延迟 | < 300ms（HK 节点覆盖全球） |
| 可用性 | 单节点 99.5%（V1），故障自动回滚镜像 |
| 部署 | CI→CD 全自动（GitHub Actions），发布即版本核对（healthz version=commit SHA） |
| 图片加载 | 对象存储直链 + CDN（腾讯云 COS） |

### 7.2 安全

| 威胁 | 防护（已实现） |
|---|---|
| 恶意上传 | MIME 白名单 + 魔数嗅探 + 5MB 限制 + 强制 image/* Content-Type |
| 存储型 XSS | 上传对象强制 Content-Disposition: inline + 禁路径穿越 |
| 钓鱼/外链滥用 | 聊天与备注禁 URL；图片仅限平台存储域名白名单 |
| 双领竞态 | SQL 条件更新（status=published）原子领取 |
| 重复发放/支付 | IAP 以 transaction_id 幂等；JWS 本地验签（Apple 根证书链） |
| 金豆超扣 | 原子 SQL 条件更新（余额足够才扣），发布失败自动回滚 |
| 接口滥用 | JWT 鉴权 + 速率限制 + 设备绑定 |

### 7.3 合规与隐私

| 项 | 状态 |
|---|---|
| 隐私政策/条款 | 英文版公网可访问（gotseeker.com） |
| GDPR | 首启同意流（不可绕过）+ 账号注销 30 天删除 |
| 隐私清单 | PrivacyInfo.xcprivacy（位置/相机/UserDefaults 等申报，不追踪） |
| 数据存储 | 香港（HK）云服务器，TLS 传输加密 |
| 支付 | V1 全部经 Apple IAP，平台零用户资金；金豆不可提现/转让 |

---

## 8. 技术架构概要

```
iOS App（SwiftUI + StoreKit 2）
        │ HTTPS / WSS
        ▼
Go 后端（Gin）─ 腾讯云 HK CVM（Docker Compose）
  ├─ SQLite（用户/任务/流水）+ Redis（验证码/会话）
  ├─ 对象存储：腾讯云 COS（图片，桶私有+对象公有读）
  ├─ 邮件：Resend API（验证码）
  └─ WebSocket Hub（实时通知/聊天）
        ▲
GitHub main ── CI（vet/build/test）── CD（SSH 部署 + 版本核对 + 失败回滚）
```

关键工程决策：发布版本可核对（GIT_SHA 编译进二进制）、机器可读错误码（i18n 英文）、**线上全链路 e2e（53 项全绿，含金豆守恒断言）**、iPhone 17 模拟器 UI 自动化测试（登录→发布→我的任务→消息）、CD-only 发布纪律（禁止手动上生产）。

---

## 9. 产品路线图

| 阶段 | 主题 | 内容 |
|---|---|---|
| **V1.0（当前，待提审）** | 英文全球首发 | IAP 金豆制闭环、LBS 任务全流程、取证与争议、聊天、通知、GDPR |
| V1.1 | 运营基建 | 任务管理（发布者编辑/删除任务）、内容审核（任务文本合规）、数据分析埋点、奖励中心落地（豆兑换权益） |
| V1.2 | 增长 | 评价/信用体系、猎人等级、推送唤醒（APNs）、推荐奖励金豆 |
| **V2.0** | 真金闭环 | 香港/美国主体注册 + Stripe Connect：赏金预授权冻结→确认分账→猎人自动 payout；金豆降级为发布门槛 |
| V2.x | 规模化 | Postgres 迁移、多区域部署、反欺诈风控、Web 端 |

---

## 10. 成功指标（KPI）

| 分类 | 指标 | V1 目标（上线 3 个月） |
|---|---|---|
| 活跃 | WAU / 发布任务数 / 完成任务数 | 验证双边密度模型 |
| 转化 | 注册→首发布 / 豆尽→付费充值 | ≥8% 付费转化 |
| 供给健康 | 任务领取中位时长 / 完成/发布比 | <30min / >60% |
| 经济健康 | 赚豆转发布占比（内循环率） | 监控 |
| 信任 | 争议率 / 自动确认占比 | <5% / 监控 |
| 变现 | IAP 收入 / ARPPU | 覆盖基础设施成本 |

---

## 11. 风险与应对

| 风险 | 等级 | 应对 |
|---|---|---|
| 双边冷启动（密度不足） | 高 | 注册礼金豆、聚焦单一城市/校园启动、5 分钟级轻任务切入 |
| 任务内容违规（涉违法/刷量） | 高 | 发布押金豆（成本前置）+ 文本审核接入（V1.1）+ 举报通道 |
| IAP 审核被拒（消耗型商品） | 中 | 已配置三档消耗型 SKU；用途说明清晰（发布赏金凭证，非货币、不可提现） |
| 真钱模式合规（V2） | 中 | 仅 Stripe Connect 官方资金流；金豆永不可提现；市场模式如实申报 |
| 单点故障 | 低 | CD 自动回滚 + 备份镜像；DB 迁移 Postgres 在路线图 |

---

*本文档与代码仓库同步维护：`README.md`（快速开始）、`ARCHITECTURE.md`（技术细节）、`TODO.md`（执行清单）、`RELEASE.md`（发布规范）、`FUNCTIONAL_TEST_CHECKLIST.md`（自测清单）。*
